use grim_macros::*;
use grim_traits::scene::{Draw, Mesh, Trans};

#[milo(Draw)]
pub struct DrawObject {

}


impl DrawObject {
    pub fn test(&self) {
        let m = self.showing;
        let mm = self.get_showing();
        //self.
        //self.
        /*milo2! {
            
        }*/
    }
}