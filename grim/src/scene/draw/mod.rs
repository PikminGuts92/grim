use grim_macros::*;
use grim_traits::scene::{Draw, Mesh, Trans};

#[milo(Draw)]
pub struct DrawObject;


impl DrawObject {
    pub fn test(&self) {
        //self.
        milo2! {
            
        }
    }
}