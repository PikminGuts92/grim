[workspace]
members = [
    "grim",
    "grim_macros",
    "grim_traits",
    "mesh_tool",
    "preview_ui",
    "scene_tool"
]

[profile.release]
codegen-units = 1
lto = true
opt-level = 'z'  # Optimize for size
